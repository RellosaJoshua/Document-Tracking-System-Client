import React from 'react';
import RequestHeader from '../../components/user/RequestHeader';

const Request = () => {
    return (
        <div className="requests-page">
            <RequestHeader />
        </div>
    );
};

export default Request;